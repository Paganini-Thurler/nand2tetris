/**
 * Or gate:
 * if (a or b) out = 1, else out = 0 
 */
CHIP Or {
    //Or input and output pins
    IN a, b;
    OUT out;

    PARTS:
    Not(in=a,out=notA);
    Not(in=b,out=notB);
    Nand(a=notA, b=notB,out=out);
}